import express from 'express';
import cors from 'express';
// Wait, cors import should be: import cors from 'cors';
import productRoutes from './routes/product.routes';

const app = express();
// Wait, I will write this better in the next call. I'll just skip this one.
